const { Client, Request, Wallet } from 'payjoin-client';

const Client = {
    Client,
    Request,
    Wallet
}
const Wallet = {
    Client,
    Request,
    Wallet
}
const Request = {
    Client,
    Request,
    Wallet
}