import client from "client";
import initVPN from "InitVPN";
import checkStatus from "CheckStatus";
import shutdown from "Shutdown";


function initVPN(): Promise<boolean> {
    

const client = {
    initVPN
    createService: null,
    checkStatus,
    shutdown
}
export default client;